# Post-Release TODO

## Features
- [ ] Add `--name` flag to CLI for tunnel labels (e.g., "bob's dev server")
- [ ] Show tunnel label on login page instead of generic message
- [ ] Default label to "tunn to [port]" if not specified
- [ ] JWT cookies for web sessions (stateless, survives restarts)

## Improvements
- [ ] Better error messages when tunnel access denied
